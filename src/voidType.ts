// the  voide type denotes the absesnce having any type at all, its juust opposist of any 

// typically  use the void type as the funtion return type that do not return a value 

const sum = (m:string): void  =>{
    console.log(m);
    
}